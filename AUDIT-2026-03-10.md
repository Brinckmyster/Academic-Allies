# Academic Allies — Nightly Audit 2026-03-10
**Auditor:** Claude
**Run time:** 2026-03-10 ~22:37 UTC
**Repo state at audit start:** commit `cd95352` (HEAD)

---

## Commits Since Last Audit (2026-03-07 evening)

| Commit | Author | Description |
|--------|--------|-------------|
| `cd95352` | Claude | Fix mode icons + Audio Notes nav |
| `d9b091c` | Claude | Connect SpoonPal to SpoonPlanner — reads tasks from spoonPlans, saves tracking to spoonPal |
| `b5e2449` | Claude | Student = Sudo, Backstage Manager = Platform Admin |
| `d6e9351` | Claude | Student is sudo over their own data — label + styling |
| `7a46189` | Claude | Restore accidentally deleted docs |
| `1561fe9` | Claude | Bump shared-header cache-bust to v=20260310a across all pages |
| `3c814a9` | Claude | Wait for persistence before auth listener — fixes sign-in button flash |
| `44cff9c` | Claude | Bump auth persistence poll to 10s, fix hero greeting name |
| `8fe88ea` | Claude | Fix double Firebase SDK loading on 14 pages |
| `8aa7232` | Claude | Fix Queen Anne's Lace genus: Daucus → Ammi (per Mary) |
| `4c5a459` | Claude | Audio Converter: default to 16kHz mono for NotebookLM |
| `c6aed30` | Claude | Add Audio Converter tool — MP3/MP4/OGG to WAV for NotebookLM |
| `8acbad8` | Claude | Major update: permission hardening, mirror fixes, mobile responsive, supporter labels |
| `93c0d72` | Claude | Spoon Planner: supporters build suggestions only + live suggestion listener + Permission stripes |
| `1044c56` | Claude | Fix auth race condition — getRedirectResult was hiding signed-in UI |
| `a393920` | Claude | Fix squashed tier icons — use height-only sizing for non-square PNGs |
| `b378d02` | Claude | Add cache-control meta to user-tiers.html to prevent stale caching |
| `b4b8f23` | Claude | Use custom badge PNGs for all tier headers in Permissions Reference |
| `3e9978b` | Claude | Fix Backstage Manager icon in Permissions Reference to use custom badge PNG |
| `79200f2` | Claude | Add tier icon PNGs to member cards + cache-control for modes |
| `7e1215e` | Claude | Fix Network Lead mode sync + icon consistency + SOP cleanup |
| `0c52196` | Claude | Migraine mode: full dimming, no undimmed bars, suggest in mirror |
| `f942f90` | Claude | Archive pre-change snapshots for SpoonPal move + template email |
| `7894548` | Claude | Fix SpoonPal visibility bug + add email to templates |
| `1e6dfc9` | Claude | Restore SpoonPal card to homepage (admin-only) |
| `87ed3f8` | Claude | Move SpoonPal off dashboard + add email to templates |
| `223b098` | Claude | Mode enforcer: modes now actually hide/show features across the app |
| `eaedd10` | Claude | Dark mode: user setting with toggle, localStorage + Firestore sync |
| `c259db9` | Claude | Counselor template: remove AA assumption, keep it neutral |
| `c005e0d` | Claude | Templates: accommodation letter, counselor outreach, network invite card with code generator |
| `050ce58` | Claude | Index cleanup: ES5, add Audio Notes + Modes cards, fix greeting, remove dupes |
| plus ~20 more | Claude | Various fixes, features, and pre-snapshot archives |

**Summary:** 55+ commits since last audit. Major features added: dark mode, mode enforcer, SpoonPal↔SpoonPlanner link, Audio Converter, Bedroom Planner, permission hardening, "Student is Sudo" paradigm, templates system. Multiple auth race condition fixes. Double Firebase SDK loading fixed across 14 pages.

---

## AUDIT ITEM 1 — Firebase SDK Consistency

**Check:** Every HTML page loads Firebase 10.7.1; no direct `initializeApp()` calls; all Firestore/Auth calls wrapped in `waitForAA()`.

**Result:** ✅ PASS
- All live pages on Firebase 10.7.1 compat SDK ✅
- `aa-firebase.js` is the sole `initializeApp()` caller, guarded by `if (!firebase.apps.length)` ✅
- Double SDK loading fix (commit `8fe88ea`) verified complete — all 14 previously-affected pages confirmed fixed with archive backups as evidence ✅
- `emergency.html` additionally loads `firebase-storage-compat.js` v10.7.1 for contact photo uploads — correct ✅
- All Firebase-using pages properly wait for AA via `waitForAA()` or `AA.auth.onAuthStateChanged()` ✅

---

## AUDIT ITEM 2 — Mirror Mode Coverage

**Check:** Pages that read from user-specific collections must use `(window.AA_MIRROR_UID || user.uid)` or `AA_uid()`.

**Result:** ✅ PASS
- checkin.html: `window.AA_MIRROR_UID || user.uid` ✅
- checkin-log.html: uses `AA_MIRROR_UID` pattern ✅
- nope-mode.html: `currentUid = window.AA_MIRROR_UID || user.uid` ✅
- emergency.html: `_studentUid = window.AA_MIRROR_UID || user.uid` ✅
- recovery-mode.html: uses `AA_uid()` function ✅
- spoon-planner.html: uses `AA_uid()` function ✅
- meal-planner.html: uses `window.AA_MIRROR_UID` ✅
- audio-notes.html: properly separates readUid (mirror) vs writeUid (self) ✅
- calendar.html: `currentUid = window.AA_MIRROR_UID || user.uid` ✅
- modes.html: uses `AA_uid()` ✅
- status-circle.js: `window.AA_MIRROR_UID || user.uid` ✅
- spoon-pal.html: correctly REJECTS mirror mode (personal planner, uses `user.uid` only) ✅
- aa-mirror.js NO_MIRROR list: `['admin.html', 'user-tiers', 'message-system', 'spoon-pal']` — appropriate ✅
- New page bedroom-planner.html: no Firestore access, no mirror needed ✅

---

## AUDIT ITEM 3 — Header Loader Correctness

**Check:** All pages using `.innerHTML` to inject shared-header.html must re-execute scripts via clone-and-replace pattern.

**Result:** ✅ PASS — all live pages use the clone-and-replace pattern correctly.

---

## AUDIT ITEM 4 — Firestore Rules Alignment

**Check:** Cross-check rules against actual page behavior; flag collections with no explicit rule.

**Result:** ✅ PASS — All 32 collections have matching Firestore rules.

| Collection | Written by | Rule present |
|------------|-----------|--------------|
| users | admin, user-tiers, aa-firebase | ✅ |
| pendingUsers | admin | ✅ |
| checkins / checkins/days | checkin | ✅ |
| nope / nope/logs | nope-mode | ✅ |
| flowerQuiz | flower quiz | ✅ |
| emergencyContacts | emergency | ✅ |
| mealPlans / mealLogs / mealLogs/days | meal-planner | ✅ |
| spoonPal / spoonPal/days | spoon-pal | ✅ |
| spoonPlans | spoon-planner | ✅ |
| spoonPlanSuggestions/pending | spoon-planner (suggestions) | ✅ |
| modeSuggestions/pending | modes (suggestions) | ✅ |
| mealSuggestions/pending | meal-planner (suggestions) | ✅ |
| recoveryJournal | recovery-mode | ✅ |
| invites | admin | ✅ |
| networkChat / group / dms / msgs | message-system | ✅ |
| messages / msgs | message-system | ✅ |
| users/{uid}/notifications | various | ✅ |
| auditLog / entries | audit-log | ✅ |
| audioNotes / notes | audio-notes | ✅ |
| deletionRequests | audio-notes | ✅ |
| messageDrafts | message-system | ✅ (tightened to owner-only since 03-08) |

messageDrafts rule from March 7 fix: present and improved — now uses UID-prefix matching (`draftId[0:request.auth.uid.size()] == request.auth.uid`) to restrict access to owner only ✅

---

## AUDIT ITEM 5 — Role/Tier Consistency

**Check:** All role strings use consistent names; TIER_ICONS match between files.

**Result:** ⚠️ WARNING (low priority)
- `TIER_ICONS` identical in shared-header.html and message-system.html ✅
- Role strings consistently use: `backstage-manager`, `network-lead`, `student`, `support`, `family`, `nearby-help`, `pending`, `archived` ✅
- No old role strings (`'admin'` as a value) in live files ✅
- ⚠️ Display labels in user-tiers.html use aliases ("Student (You — Sudo)" and "Backstage Manager (Platform Admin)") that don't appear in other pages. Cosmetic inconsistency only — role strings themselves are correct.

---

## AUDIT ITEM 6 — Broken Links / Missing Files

**Check:** Links in modular/*.html; script src and link href targets; icons referenced in TIER_ICONS.

**Result:** ✅ PASS
- All 6 tier icons exist in `modular/icons/` ✅
- All script files exist (aa-firebase.js, aa-mirror.js, dark-mode.js, migraine-mode.js, mode-enforcer.js, draggable.js, status-circle.js) ✅
- No dead `<a href>` links to nonexistent pages in live files ✅
- `modular/styles.css` referenced only in archived files (not live) — no impact ✅
- New pages (audio-converter.html, bedroom-planner.html) properly linked ✅

---

## AUDIT ITEM 7 — JavaScript Errors / Dead Code

**Check:** Obvious JS errors; TODO/FIXME comments; console.error with internal paths.

**Result:** ⚠️ WARNING
- `audio-notes.html` line ~997: `// TODO: future — integrate Otter.ai or OpenAI Whisper API` — acknowledged future enhancement, not blocking ✅
- No unclosed functions, undefined variable references, or duplicate declarations found ✅
- No `alert()` calls exposing internal paths in user-facing pages ✅
- `alert()` calls present in admin-only pages (user-tiers.html, utc-converter.html) — acceptable for admin tools ✅

---

## AUDIT ITEM 8 — Security / Data Leakage

**Check:** PII not exposed cross-user; emergency contacts protected; checkin data scoped.

**Result:** ⚠️ WARNING (medium priority)
- Emergency contacts rule: only owner, network-lead, or backstage-manager can read ✅
- Checkins rule: only owner, network members, or backstage-manager ✅
- SpoonPal: owner/admin only (no network read — private) ✅
- All innerHTML injections use `esc()` sanitization function — no XSS vectors ✅
- No hardcoded credentials beyond Firebase client config (client-side API key is expected/normal for Firebase) ✅
- ⚠️ **Console PII exposure:** `aa-firebase.js` logs user email addresses in several `console.log()` calls during authentication (lines ~214, 219, 293, 306, 309, 329). These are debug-level logs but expose email addresses in browser console in production. Low risk (only visible to the user themselves in DevTools) but not best practice.

---

## AUDIT ITEM 9 — Archive Hygiene

**Check:** Pre-snapshot files should be in `modular/archive/` or use `.archive-` suffix, not sit alongside live files.

**Result:** ❌ FAIL — 9 misplaced pre-prefix files found.

| File | Location | Should be |
|------|----------|-----------|
| `aa-firebase-pre-persist-20260304.js` | modular/ | modular/archive/ |
| `aa-firebase-pre-redirect-20260304.js` | modular/ | modular/archive/ |
| `shared-header-pre-persist-20260304.html` | modular/ | modular/archive/ |
| `shared-header-pre-redirect-20260304.html` | modular/ | modular/archive/ |
| `shared-header-pre-redirectfix-20260304.html` | modular/ | modular/archive/ |
| `audit-log-pre-autoselect-20260305.html` | components/audit-log/ | modular/archive/ |
| `audit-log-pre-headerfix-20260304.html` | components/audit-log/ | modular/archive/ |
| `meal-planner-pre-presignout-20260306.html` | components/meal-planner/ | modular/archive/ |
| `message-system-pre-presignout-20260306.html` | components/message-system/ | modular/archive/ |

Additionally, 3 root-level `.bak` files from August 2025 could be archived: `meal-planner-local.bak`, `universal-suggestor-local.bak`, `universal-suggestor.js.bak`.

---

## AUDIT ITEM 10 — New Page Integration

**Check:** New pages added since March 7 have proper header loading and Firebase integration.

**Result:** ✅ PASS (with note)
- Audio Converter: header loading ✅, no Firebase needed (client-side tool) ✅
- Bedroom Planner: standalone layout tool, no header/Firebase — ⚠️ intentional standalone page, but lacks shared header navigation. Users must navigate back manually.
- All other new features (SpoonPal↔SpoonPlanner link, dark mode, mode enforcer, templates) properly integrated ✅

---

## Summary

| Check | Status |
|-------|--------|
| Firebase SDK 10.7.1 everywhere | ✅ |
| No duplicate initializeApp() | ✅ |
| Double SDK loading fix verified | ✅ |
| Mirror mode UID guards (12 pages) | ✅ |
| Header clone-and-replace on all pages | ✅ |
| Firestore rules: all 32 collections covered | ✅ |
| messageDrafts rule tightened to owner-only | ✅ |
| Role/tier naming consistency | ✅ (display label alias warning) |
| TIER_ICONS match shared-header ↔ message-system | ✅ |
| No broken links or missing assets | ✅ |
| No JS errors in live pages | ✅ |
| No console PII leakage | ⚠️ (email in auth debug logs) |
| Emergency contacts protected | ✅ |
| XSS protection (esc() sanitization) | ✅ |
| **Archive hygiene: 9 misplaced files** | **❌ FAIL** |
| New page integration | ✅ |
| All priority features complete | ✅ |

**Audit grade: MOSTLY CLEAN** — 1 open failure (archive hygiene, 9 files need moving). 2 warnings (console PII, display label aliases). No security or functional failures.

---

## Recommended Actions (not applied during this audit)

1. **Archive hygiene (❌):** Move the 9 misplaced pre-prefix files to `modular/archive/`. Move 3 root `.bak` files to archive or remove.
2. **Console PII (⚠️):** Consider wrapping email-logging `console.log` calls in `aa-firebase.js` with a debug flag so they can be disabled in production.
3. **Display labels (⚠️):** Decide whether "Sudo" and "Platform Admin" aliases should appear consistently across all pages or only in user-tiers.html.
4. **Bedroom Planner:** Consider adding shared header navigation for consistency, or document it as an intentionally standalone tool.

---

*Audit by Claude — Academic Allies nightly audit system*
