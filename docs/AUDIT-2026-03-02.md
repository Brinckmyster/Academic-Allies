# Academic Allies — Nightly Deep Audit
**Date:** 2026-03-02
**Run by:** Claude (automated nightly task)
**Repo:** /Academic-Allies (main branch)

---

## ✅ PASS

- **Firebase SDK Consistency:** All pages (checkin.html, checkin-log.html, emergency.html, nope-mode.html, admin.html, accommodations.html, resources.html, bad-brain-day.html, recovery-mode.html, spoon-planner.html, modes.html) load Firebase 10.7.1 compat scripts. No direct `firebase.initializeApp()` calls outside aa-firebase.js. Pages that use Firestore/Auth use `waitForAA()` or rely on shared-header's auth flow.

- **Mirror Mode Coverage:** All pages reading from `checkins/{uid}` or `nope/{uid}` correctly use `window.AA_MIRROR_UID || user.uid` as the UID. Key pages verified: checkin-log.html (AA_MIRROR_UID guard), nope-mode.html (currentUid = AA_MIRROR_UID || user.uid), emergency.html (_studentUid = AA_MIRROR_UID || user.uid). Mirror write-blocks are in place on nope-mode.html.

- **Header Loader Correctness:** All pages that inject shared-header.html use the correct clone-and-replace pattern: `innerHTML` + `querySelectorAll('script')` loop with `old.replaceWith(s)`. Verified: checkin.html, checkin-log.html, admin.html, emergency.html, accommodations.html, resources.html, support-dashboard.html, user-tiers.html, modes.html. No broken innerHTML-only patterns found.

- **Firestore Rules Alignment:** All collections written to by the app have explicit rules. Verified: `users`, `checkins`, `nope`, `emergencyContacts`, `inviteCodes` (invites), `messages`, `networkChat`, `mealPlans`, `mealLogs`, `spoonPal`, `spoonPlans`, `recoveryJournal`, `flowerQuiz`, `pendingUsers`. Rules match actual page write patterns.

- **Role/Tier Consistency:** TIER_ICONS maps in shared-header.html and message-system.html are identical (`backstage-manager → badge-admin-yellow-center.png`, `network-lead → icon-user-admin.png`). Both icon files confirmed present in modular/icons/. No old `'admin'` role strings found in active (non-archive) files.

- **Broken Links / Missing Files:** All `<a href>` links in active HTML pages resolve to existing files. All 13 linked HTML pages confirmed present. No missing script or stylesheet references.

- **Security / Data Leakage:** `emergencyContacts` Firestore rule restricts reads to owner, network-lead, or backstage-manager — correct. Check-in data readable by all signed-in users (support network reads logs — per design). No PII exposed to arbitrary users.

- **Prior audit FAILs:** All 6 FAILs from 2026-03-01 remain resolved: admin.html Firebase SDK, accommodations.html template, resources.html template, support-dashboard.html flower total, checkin.html aa-mirror.js cache-bust.

- **Priority 2 (from 2026-03-01):** emergency.html — Seed Default Contacts button and Pick from Support Network panel both present and correct. ✅

- **message-system.html timestamps:** Timestamps ARE displayed on messages using `fmtTime()` in `.msg-meta` — Priority 3 message item is already complete. ✅

---

## ⚠️ WARNING

- **shared-header.html line 59** | `badge-admin-icon.png` referenced via absolute GitHub URL (`https://brinckmyster.github.io/Academic-Allies/modular/icons/badge-admin-icon.png`). Carryover from 2026-03-01. Will break if repo is moved. **FIXING in Phase 2 today.**

- **nope-mode.html ~lines 303–310** | Nope notification box shows hardcoded support contact names instead of reading from Firestore. Not a security issue. Complex to fix — flagged for Bruise review (carryover).

- **aa-firebase.js line 129** | `TODO (Play Store launch): Change 'pending' → 'student' in this function` — pending release decision. Not a bug today.

---

## ❌ FAIL

*None found in today's pass.*

---

## Phase 2 — Fixes Applied Today

### FIX-1 — shared-header.html: absolute URL → relative path
- **File:** `modular/shared-header.html` line 59
- **Issue:** `badge-admin-icon.png` loaded via absolute `https://brinckmyster.github.io/...` URL.
- **Fix:** Changed to `/Academic-Allies/modular/icons/badge-admin-icon.png` (site-root-relative).
- **Status:** ✅ FIXED (archived as `shared-header.html.archive-20260302-abs-url`)

---

## Phase 3 — Feature Work

### PRIORITY 3 — checkin.html: Custom check-in prompts from studentProfile.checkinPrompts
- **Feature:** After auth, the app reads `users/{uid}.studentProfile.checkinPrompts` (string[]).
- **Behavior:** If the array has entries, they replace the default `.gateway-q` text for each section (mental, physical, school, spiritual, social in that order). Partial arrays are safe — categories beyond the array length keep their defaults.
- **Status:** ✅ IMPLEMENTED (archived as `checkin.html.archive-20260302-custom-prompts`)

---

*Audit written by Claude — 2026-03-02*
