# Academic Allies — Nightly Deep Audit
**Date:** 2026-03-03
**Run by:** Claude (automated nightly task)
**Repo:** /Academic-Allies (main branch)

---

## ✅ PASS

- **Firebase SDK Consistency (pages with direct loading):** All pages that load Firebase directly in `<head>` use 10.7.1. No direct `firebase.initializeApp()` calls outside aa-firebase.js. All Firestore/Auth calls wrapped in `waitForAA()` or equivalent. ✅

- **Mirror Mode Coverage:** All pages reading from `checkins/{uid}` or `nope/{uid}` correctly use `window.AA_MIRROR_UID || user.uid`. Key pages verified: checkin.html (line 903), checkin-log.html (line 410–412). Pages without Firestore reads (accommodations, resources, modes) do not need mirror guards. aa-mirror.js NO_MIRROR list correctly includes: `admin.html`, `user-tiers`, `message-system`, `spoon-pal`. ✅

- **Header Loader Correctness:** All pages that inject shared-header.html use the correct clone-and-replace pattern (`innerHTML` + `querySelectorAll('script')` loop with `old.replaceWith(s)`). Verified: checkin.html, modes.html, accommodations.html, resources.html, calendar.html. No broken innerHTML-only patterns found. ✅

- **Firestore Rules Alignment:** All collections written to by the app have explicit rules. Verified: `users`, `checkins`, `nope`, `emergencyContacts`, `invites`, `messages`, `networkChat`, `mealPlans`, `mealLogs`, `spoonPal`, `spoonPlans`, `recoveryJournal`, `flowerQuiz`, `pendingUsers`. Rules match actual page write patterns. ✅

- **Role/Tier Consistency:** TIER_ICONS maps in shared-header.html and message-system.html are identical (`backstage-manager → badge-admin-yellow-center.png`, `network-lead → icon-user-admin.png`). Both icon files confirmed present in modular/icons/. Only one old `'admin'` occurrence found — in aa-firebase.js as a comment string in the file header (not a code reference). ✅

- **Broken Links / Missing Files:** All `<a href>` links in sitemap verified:
  - meal-planner-mary/index.html ✅
  - spoon-planner/spoon-planner.html ✅
  - spoon-planner/spoon-pal.html ✅
  - static/docs/TEAM-PROFILES.html ✅
  - All other core pages confirmed present. ✅

- **JavaScript Errors / Dead Code:** No unclosed functions or obvious syntax errors found. The only TODO is in aa-firebase.js line 132 (Play Store launch pre-condition, pre-existing, not actionable today). No `alert()` calls that expose internal paths — admin.html alert() calls are gated behind the backstage-manager role and surface only generic error messages to the admin user. ✅

- **Security / Data Leakage:** `emergencyContacts` Firestore rule restricts reads to owner, network-lead, or backstage-manager only. Check-in data readable by all signed-in users (support network reads — by design). No PII exposure to arbitrary users identified. ✅

- **Prior audit FAILs (2026-03-02):** All resolved. shared-header.html absolute URL fixed to site-root-relative path. ✅

- **Priority Features Complete (carried from prior audits):**
  - Modes tile navigation → links working via `<a href>` elements (2026-03-01) ✅
  - accommodations.html / resources.html → AA standard page template (2026-03-01) ✅
  - Emergency contacts seed button → backstage-manager only (2026-03-01) ✅
  - Pick from support network in emergency.html → reads supportNetwork, pre-fills (2026-03-01) ✅
  - Custom check-in prompts (studentProfile.checkinPrompts) → checkin.html (2026-03-02) ✅
  - Message timestamps → displayed via fmtTime() in .msg-meta (verified present) ✅

---

## ⚠️ WARNING

- **nope-mode.html ~lines 303–310** | Nope notification box shows hardcoded support contact names instead of reading from Firestore. Not a security issue. Complex to fix — **flagged for Bruise review** (carryover from 2026-03-01/02). No action today.

- **aa-firebase.js line 132** | `TODO (Play Store launch): Change 'pending' → 'student'` — pending release decision. Not a bug today. **Flagged for Bruise review** (carryover).

---

## ❌ FAIL

### FAIL-1 — shared-header.html line 84: Firebase dynamic loader using version 9.23.0
- **File:** `modular/shared-header.html` line 84
- **Issue:** `var FB_BASE = 'https://www.gstatic.com/firebasejs/9.23.0/'` — the shared header's conditional Firebase loader uses 9.23.0. Pages that do NOT load Firebase directly in their own `<head>` (calendar.html, support-dashboard.html, user-tiers.html, message-system.html) receive Firebase 9.23.0 via this dynamic loader, violating the requirement that all pages use 10.7.1.
- **Status:** ✅ **FIXED in Phase 2 today** (see below)

---

## Phase 2 — Fixes Applied Today

### FIX-1 — shared-header.html: Firebase dynamic loader 9.23.0 → 10.7.1
- **File:** `modular/shared-header.html` lines 84–88
- **Issue:** `FB_BASE` pointed to Firebase 9.23.0. Pages relying on shared-header for Firebase received the wrong version.
- **Fix:** Changed `FB_BASE` to `'https://www.gstatic.com/firebasejs/10.7.1/'`. Also bumped aa-firebase.js cache-bust version string from `v=20260301a` → `v=20260303a` to force fresh load.
- **Archive:** `shared-header.html.archive-20260303-fb-version`
- **Status:** ✅ FIXED

---

## Phase 3 — Feature Work

All Priority 1, 2, and 3 items from the task list are complete as of previous runs. No new feature work needed today.

---

*Audit written by Claude — 2026-03-03*
